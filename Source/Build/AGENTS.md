# Правила проекта

- Работай только с `Build.html` и `src/`.
- `Build.standalone.html` читать и редактировать нельзя, это тяжелый файо сборки.
- `build.py` это сборщик его не трать!. 
- Если не хватает важных вводных, сначала задай уточняющий вопрос.
- Для интернета используй минимум поиска, только если это реально нужно.
